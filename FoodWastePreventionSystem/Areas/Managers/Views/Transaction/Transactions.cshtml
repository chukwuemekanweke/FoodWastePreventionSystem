@model List<TransactionVM>
@{
    ViewBag.Title = "Sales";
    List<Transaction> Transactions = Model.Select(x => x.Transaction).ToList();
}

<div class="row">
    <div class="col-md-12">
        <div class="card-box">
            <div class="card-box-head  border-b m-t-0">
                <h4 class="header-title"><b>Transactions</b> <span class="pull-right m-l-5"><i class="fa fa-cog"></i></span></h4>
            </div>
            <div class="card-box-content form-compoenent">
                <div class="cb-res-table">
                    <div class="cb-table-search">
                      
                        <div class="input-group pull-right cb-ta-search">
                            @using (Ajax.BeginForm("GetTransactionsForProduct", "Transaction", new{ type = ViewBag.TransactionType},new AjaxOptions() {
                                InsertionMode=InsertionMode.Replace,
                                HttpMethod = "Get",
                                UpdateTargetId = "tablePlaceholder",
                                AllowCache = true,
                            }))
                            {
                                @Html.DropDownList("id", (List<SelectListItem>)ViewBag.Products, "Select A Product", new { @class = "form-control", id = "product" })

                                <span class="input-group-btn">
                                    <button class="btn btn-default" type="submit">Go!</button>
                                </span>
                            }
                        </div>
                    </div>
                </div>
                <div class="clearfix"></div>
                <br>
                <div class="table-responsive data-table" id="tablePlaceholder">
                    @Html.Partial("_SalesTable", Model)
                </div>
                <div class="row mob-center">
                    <div class="col-sm-5">
                        <p>Showing 20-30 of 50 items</p>
                    </div>
                    <div class="col-sm-7">
                        <ul class="pagination pull-right">
                            <li><a href="#"><span class="fa fa-angle-double-left"></span></a></li>
                            <li class="active"><a href="#">1</a></li>
                            <li><a href="#">2</a></li>
                            <li><a href="#">3</a></li>
                            <li><a href="#">4</a></li>
                            <li><a href="#">5</a></li>
                            <li><a href="#"><span class="fa fa-angle-double-right"></span></a></li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
